# Anonymity and Release Policy

V1 contains no private benchmark outputs and no paper submission package.

Before any public or anonymous release:

- remove private notes and prompt logs;
- verify generated artifacts carry the correct evidence status;
- exclude unverified datasets or samples;
- include licenses or source notes for every registry item;
- rerun the final audit.
