# V5 Command Bundles

`NO_REAL_EVIDENCE`

Scripts live in `commands/v5/`.

Each script uses `set -euo pipefail`, avoids local absolute paths, and requires environment variables for real inputs. Outputs are non-claim unless later evidence gates pass.

