# Control Artifact Protocol

`certgen prepare controls` materializes prospectively allocated null reference splits and the fixed obvious-gap Gaussian-blur pair. Null sources are disjoint. Obvious-gap clean/corrupt outputs share sources, retain distinct output IDs, use RGB/native dimensions, and freeze radius 2.0 without outcome tuning. Integrity and image manifests bind every byte; all outputs remain claim-ineligible inputs.
