# Certificate-Input Bundle Contract

The canonical builder emits one immutable NPZ plus sidecar and validation result per frozen family hypothesis. Model and control roles come only from validated cache-v2 groups. Bundles bind study, family, comparison, feature, metric, kernel, bandwidth, budget, role manifests, preprocessing, extractor, caches, alpha, and the materialized reference draw order. Missing roles, mixed contracts, insufficient rows, post-hoc members, or unregistered caches fail closed.
