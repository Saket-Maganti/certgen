# Result Injection Protocol V5

`NO_REAL_EVIDENCE`

Result injection fails unless the analysis-plan hash matches, claim traces are complete, result artifacts exist, FID-sensitive claims pass the FID policy, and evidence status is sufficient.

Before real runs, injection retains `TBD_REAL_RUN_REQUIRED` and writes `claim_allowed=false`.

