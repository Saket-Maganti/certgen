# Cross-Feature Consensus Policy

Direct consensus requires every registered valid primary feature lane to decide the same direction. Opposite directions are representation-specific findings, not automatically implementation defects when contracts match. A decided lane plus an unresolved lane cannot form consensus. An invalid lane blocks consensus eligibility. Every representation-specific edge names its feature space and certificate lineage. The policy was frozen before real outcomes.
