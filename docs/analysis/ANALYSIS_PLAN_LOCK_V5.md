# Analysis Plan Lock V5

`NO_REAL_EVIDENCE`

The locked analysis plan lives at `data/contracts/analysis_plan_lock_v5.json`.

The lock hash is written to `data/results/analysis_plan_lock_hash_v5.txt`.

Result injection must stop if the current plan hash differs from the recorded hash, unless an explicit amendment is marked `posthoc=true` and `paper_claim_scope=exploratory`.

