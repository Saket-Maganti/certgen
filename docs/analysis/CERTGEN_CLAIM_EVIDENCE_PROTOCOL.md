# Claim-Evidence Protocol

The reviewer matrix binds every intended statement to required artifacts, controls, representations, budgets, paper location, limitations, current status, and claim permission. Before real runs every empirical row is blocked and false. `validate claims` fails closed on missing fields, promoted empirical status, or true claim permission. The paper firewall consumes this validation, so a malformed or promoted matrix blocks paper injection.
