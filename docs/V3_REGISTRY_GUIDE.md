# V3 Registry Guide

`NO_REAL_EVIDENCE`

The V3 registry tracks benchmarks, model pairs, feature caches, reported claims, and pilot eligibility. Use `not_checked` or `availability_planned` until metadata is verified. Do not mark rows `pilot_ready` unless all gates pass.
