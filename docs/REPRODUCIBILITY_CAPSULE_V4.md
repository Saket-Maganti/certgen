# Reproducibility Capsule V4

`NO_REAL_EVIDENCE`

The capsule lists commands, dependencies, registry templates, feature-cache schemas, preprocessing locks, run plans, certificate configs, report commands, evidence policy, and limitations.
