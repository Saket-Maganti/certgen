# Provenance Ledger Guide

`NO_REAL_EVIDENCE`

Fill the released-sample ledger manually from verified public/free sources. Do not fake rows, scores, sample counts, URLs, licenses, checkpoints, or feature-cache paths.

Validation confirms availability metadata only. It does not produce benchmark evidence and always emits `claim_allowed: false`.
