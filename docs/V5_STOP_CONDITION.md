# V5 Stop Condition

`NO_REAL_EVIDENCE`

After V5, the next step is real execution, not V6 infrastructure.

Do not build generic V6 infrastructure unless a real execution attempt exposes a concrete missing piece: provenance schema failure, feature-cache mismatch, metric reproduction failure, certificate scale failure, result-injection contract mismatch, paper-build failure, or release-safety failure.

