# Preprocessing Locks V4

`NO_REAL_EVIDENCE`

Preprocessing locks record resize, interpolation, crop, normalization, extractor version, dtype, batch size, sample order, and reference-set policy. Unknown preprocessing blocks claim eligibility.
