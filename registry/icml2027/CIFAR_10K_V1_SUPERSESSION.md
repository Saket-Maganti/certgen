# CIFAR 10k v1 supersession record

`icml2027_cifar_confirmatory_10k_v1` is `SUPERSEDED_BEFORE_EXECUTION` and
`DO_NOT_RUN`. Its frozen YAML is retained unchanged for provenance. The v1
contract specified without-replacement sampling from a finite reference pool;
that dependence is not covered by the confirmatory confidence-sequence proof.

The only execution candidate is `icml2027_cifar_confirmatory_10k_v2`, which
uses the precommitted, deterministic IID-with-replacement draw plan registered
at `registry/manifests/icml2027/cifar10_reference_draw_plan_10k_v2.json`.

No legacy pilot outcome was inspected while choosing this correction.
`claim_allowed=false`.
