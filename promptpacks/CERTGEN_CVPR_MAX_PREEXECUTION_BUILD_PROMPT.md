# CERTGEN — CVPR-FIRST MAXIMUM PRE-EXECUTION BUILD, SCIENTIFIC REPAIR, EXPERIMENT FACTORY, KAGGLE T4×2 RUNBOOKS, AND FINAL RUN MANIFEST

You are GPT-5.6 Sol operating as:

- a senior computer-vision researcher;
- a generative-model evaluation researcher;
- a sequential-inference specialist;
- a research software architect;
- a Kaggle GPU workflow engineer;
- a CVPR paper and reproducibility reviewer;
- an adversarial evidence-integrity auditor.

You have full access to the local repository:

```text
/Users/saketmaganti/Projects/certGen
```

Your task is to transform CertGen from its current audited, pre-empirical state into the strongest scientifically defensible **CVPR-first, pre-execution research system** possible **without running any real dataset, checkpoint, model-generation, feature-extraction, certificate, ranking, literature-audit, or paper-result experiment**.

This is the final intended major build pass before real execution.

Do not create another generic V10/V11 prompt-pack layer. Work directly on the live repository. Inspect, repair, consolidate, implement, test, document, and finish the actual system.

The goal is not to make the repository larger. The goal is to make CertGen materially closer to a credible CVPR main-conference submission once the real runs are executed.

---

# 1. Current project context

The latest forensic audit reported the top-level status:

```text
LOCAL_RESEARCH_CORE_VALID_BLOCKED_BY_REFERENCE_INPUT
```

The reported local verification included:

```text
212 tests passed
31 statistical-lane tests passed
25 artifact-contract tests passed
8/8 forensic-audit checks passed
```

The live repository, executable code, tests, manifests, paper sources, and current artifact state are the source of truth. Reproduce or correct every reported fact.

The current defensible statistical route is intentionally narrow:

- fixed and prospectively declared evaluation protocol;
- L2-normalized feature vectors where required;
- bounded RBF kernel;
- direct pairwise MMD-squared difference contributions;
- conservative support bound in `[-3,3]`;
- non-overlapping sample units;
- union-Hoeffding time-uniform confidence sequence;
- first-boundary-crossing directional decision;
- Bonferroni simultaneous family control;
- FID/FD treated descriptively unless separately justified;
- polynomial KID not automatically certified;
- unsupported betting-grid, empirical-Bernstein, and directional e-BH routes blocked or demoted.

The current repository has no validated real scientific evidence unless actual artifacts prove otherwise.

Expected absent evidence includes:

- no validated CIFAR-10 reference package;
- no completed checkpoint real-load preflight;
- no real generated-sample archive;
- no real Inception feature cache;
- no real CLIP feature cache;
- no real DINO feature cache;
- no real metric reproduction result;
- no real null/obvious-gap validation;
- no real certificate card;
- no real undecided fraction;
- no real samples-to-decision result;
- no real certified ranking graph;
- no paper evidence with `claim_allowed=true`.

Preserve this boundary.

---

# 2. Primary objective

Build the complete pre-execution foundation for a **CVPR-first visual generative-model evaluation paper** whose strongest possible eventual message is:

> Conventional point-estimate leaderboards for visual generative models often imply a total ranking that the available image samples do not statistically support. CertGen replaces that unsupported total order with optional-stopping-valid pairwise decisions, samples-to-decision, and certified partial rankings across visual feature spaces and model families.

The project should be prepared to support, after real execution:

1. a rigorous bounded-RBF certificate core;
2. a modern visual-generation evaluation audit;
3. certified partial rankings rather than forced total rankings;
4. cross-representation comparison using Inception, CLIP, and DINO-style features;
5. samples-to-decision and compute-budget analysis;
6. protocol-sensitivity analysis;
7. visual failure galleries and representative model-pair panels;
8. prospective literature/leaderboard auditing;
9. multiple image benchmarks and recognizable model families;
10. optional video-generation extension only if the image core is successful.

Do not claim any of these empirical findings before real runs.

---

# 3. CVPR-first research identity

The final system must be structured around a vision-native paper identity.

## 3.1 Primary identity

CertGen should be built as:

> A visual generative-model evaluation and certified-ranking framework that reveals which model comparisons are resolved, unresolved, contradictory across feature spaces, or unnecessarily expensive under fixed evaluation budgets.

## 3.2 Secondary identity

The secondary identity is:

> A statistically disciplined decision layer for bounded kernel-based generative-model comparisons under continuous monitoring.

## 3.3 Do not frame CertGen as

- a new generative model;
- a new visual feature extractor;
- a replacement for FID, CMMD, DINO-based metrics, or precision/recall;
- a universal metric-agnostic theorem;
- a general sequential-inference theory paper;
- a paper proving that FID is useless;
- a paper whose contribution is repository size, tests, prompt packs, or runbooks;
- a CIFAR-only paper;
- a paper that forces a total ranking where the evidence supports only a partial order.

## 3.4 CVPR-specific success condition

The built system must make it possible for the eventual paper to answer visually meaningful questions such as:

- Which visual generative-model comparisons are actually decided?
- Which leaderboard edges remain unresolved?
- Which decisions are consistent across Inception, CLIP, and DINO representations?
- Which model pairs reverse direction across feature spaces?
- Which comparisons need far fewer than 50k samples?
- Which comparisons remain unresolved even at large budgets?
- Which reported visual improvements disappear under valid family-wise control?
- Which preprocessing choices alter the certified conclusion?
- What does an evidence-supported partial leaderboard look like?

---

# 4. Non-negotiable evidence and safety rules

## 4.1 No real experiments in this build pass

Do not:

- download CIFAR or other datasets automatically;
- download model checkpoints automatically;
- run Kaggle or Colab;
- run GPU generation;
- run real feature extraction;
- import or manufacture fake copied-back outputs;
- run real metric reproduction;
- run real certificates;
- produce empirical figures;
- populate paper result tables;
- compute a real undecided fraction;
- promote any artifact to paper evidence;
- set `claim_allowed=true`.

Fixture and synthetic validation are allowed only when explicitly labeled.

## 4.2 Evidence labels

Use explicit labels:

```text
planning_only
not_empirical_evidence
claim_allowed=false
```

```text
synthetic_validation_only
not_model_evidence
claim_allowed=false
```

```text
non_evidence_preflight
run_log_only
claim_allowed=false
```

```text
cache_artifact
not_yet_paper_evidence
claim_allowed=false
```

```text
pilot_only
single_benchmark_only
not_generalized
claim_allowed=false
```

## 4.3 Repository safety

Before editing:

- record branch and current commit;
- record `git status --short`;
- record modified, staged, untracked, and ignored files;
- identify large files;
- identify cache and generated directories;
- preserve raw ZIPs and manifests;
- do not reset, clean, rebase, force-checkout, delete, or overwrite unique artifacts;
- do not commit or push unless explicitly instructed;
- do not upload anything;
- do not expose secrets or private absolute paths in public-facing artifacts.

## 4.4 Scientific honesty

Do not:

- fabricate citations;
- fabricate model availability;
- fabricate benchmark support;
- fabricate runtimes as measured values;
- describe a notebook as errorless before real execution;
- call static analysis an empirical validation;
- call checkpoint metadata a successful model load;
- infer a paper claim from synthetic results.

Runtime values may be provided only as:

```text
planning estimate
hardware-dependent
not an empirical project result
```

---

# 5. Required mode of operation

Work in this order:

1. inspect;
2. reproduce baseline;
3. identify CVPR-readiness gaps;
4. repair correctness defects;
5. consolidate architecture;
6. build missing experiment systems;
7. build hardened Kaggle notebooks;
8. add fixture/synthetic tests;
9. run local-safe validation;
10. build the final execution handbook;
11. produce the final CVPR pre-execution audit.

Do not start by writing broad reports without inspecting source code.

For every material finding classify it as:

```text
VERIFIED_CURRENT
VERIFIED_BUT_STALE
REPORTED_NOT_REPRODUCED
CONTRADICTED_BY_REPOSITORY
PLANNING_ONLY
SYNTHETIC_ONLY
RUN_LOG_ONLY
CACHE_ARTIFACT
SANITY_ARTIFACT
PILOT_ARTIFACT
PAPER_EVIDENCE
MISSING
BLOCKED
```

---

# 6. Phase A — Reproduce and inventory the live repository

Inspect:

```text
README*
pyproject.toml
setup.cfg
setup.py
requirements*.txt
environment*.yml
Makefile
.github/
certgen/
tests/
commands/
configs/
notebooks/
docs/
paper/
registry/
data/
results/
reports/
release/
```

Run all local-safe canonical checks available:

- Python compilation;
- package import checks;
- full default pytest suite;
- statistical test lane;
- artifact-contract lane;
- forensic audit;
- final execution audit;
- notebook static analysis;
- paper firewall;
- release scan;
- secrets scan;
- `ruff`;
- `mypy`, if configured;
- paper compilation;
- `git diff --check`.

Record:

```text
command
working directory
environment
start time
end time
exit code
duration
passed
failed
skipped
warnings
output artifact
```

Create or update:

```text
reports/CERTGEN_CVPR_BASELINE_REPRODUCTION.md
reports/CERTGEN_CVPR_COMMAND_LEDGER.csv
reports/CERTGEN_CVPR_CURRENT_STATE.json
```

Determine whether the reported 212-test state is still correct.

If recursive tests invoke the full suite from inside the suite, repair the test architecture so:

- normal unit tests do not recursively launch the entire suite;
- audit integration tests remain explicit;
- CI lanes are non-recursive;
- historical audit compatibility is preserved where reasonable.

---

# 7. Phase B — Audit and repair the statistical core

The existing forensic audit narrowed the valid route. Verify the implementation directly.

## 7.1 Formal stream

Document the exact implemented per-step contribution stream for comparing model A and model B against reference R.

Verify:

- sample roles;
- number of independent samples per time step;
- shared reference use;
- cancellation of reference-reference terms;
- non-overlapping construction;
- filtration;
- stopping boundary;
- sign convention;
- tie behavior;
- maximum budget;
- family size;
- alpha allocation.

## 7.2 Boundedness

Verify the exact support bound.

For an RBF kernel in `[0,1]`, determine whether the implemented direct difference contribution is correctly bounded in:

```text
[-3,3]
```

or another interval.

Add deterministic and property-based tests for:

- lower bound;
- upper bound;
- random normalized features;
- identical features;
- extreme kernel values;
- clipping behavior;
- invalid values;
- NaNs and infinities.

## 7.3 Dependence and sample construction

Verify that the real-run plan can satisfy:

- non-overlapping A samples;
- non-overlapping B samples;
- non-overlapping or otherwise theory-compatible reference samples;
- no accidental reuse caused by shard merging;
- no adaptive bandwidth tuning on the certificate stream;
- no post hoc pair selection without multiplicity accounting.

If the current reference sampling plan is not operationally enforceable, repair it.

Create a deterministic reference-draw plan artifact containing:

```text
run_id
benchmark
reference_population_hash
draw_method
with_or_without_replacement
seed
sample_ids
pairing_structure
time_index
role
```

## 7.4 Time-uniform confidence sequence

Verify the union-Hoeffding implementation:

- alpha allocation across time;
- finite/infinite horizon convention;
- one-sided and two-sided forms;
- interval transformation;
- support-width scaling;
- numerical stability;
- first-crossing logic;
- no repeated alpha spending after resume;
- deterministic restart behavior.

Add tests for:

- null streams;
- constant positive streams;
- constant negative streams;
- small effects;
- resumed streams;
- interrupted streams;
- maximum-budget undecided outcomes;
- false-direction prevention.

## 7.5 Multiplicity

Verify Bonferroni family construction across:

- model pairs;
- feature spaces;
- metrics;
- benchmarks;
- budgets;
- preprocessing variants;
- kernels.

The family must be prospectively frozen for any claim-bearing analysis.

Implement a family registry containing:

```text
family_id
analysis_scope
benchmark
feature_space
metric
kernel
bandwidth
model_pairs
alpha_total
number_of_hypotheses
alpha_per_hypothesis
status
preregistered_at
configuration_hash
```

Do not claim global ranking validity from ad hoc pairwise tests.

## 7.6 Capability registry

Create or update:

```text
docs/theory/CERTGEN_METRIC_CAPABILITY_REGISTRY.csv
```

with:

```text
metric
representation
descriptive_supported
fixed_sample_inference_supported
anytime_certificate_supported
required_assumptions
boundedness
dependence_status
current_implementation
paper_claim_status
```

Ensure:

- bounded RBF-MMD route is the rigorous core;
- CMMD-like analysis is described as bounded RBF-MMD on CLIP features when applicable;
- DINO-based distance use is described precisely;
- FID/FD remain descriptive unless separately validated;
- polynomial KID is not certified by inheritance;
- precision/recall and density/coverage are not called certified unless a valid procedure exists.

## 7.7 Statistical validation suite

Build two lanes:

### Fast CI statistical lane

- no internet;
- no dataset;
- no GPU;
- short deterministic or low-repetition tests.

### Extended synthetic validation lane

- optional;
- CPU only;
- controlled repetitions;
- Monte Carlo uncertainty;
- validates type-I behavior under optional stopping;
- validates direction under alternatives;
- validates censoring and samples-to-decision logic;
- clearly labeled synthetic-only.

Create:

```text
docs/theory/CERTGEN_CVPR_STATISTICAL_CORE_AUDIT.md
docs/theory/CERTGEN_FORMAL_STREAM_CONTRACT.md
docs/theory/CERTGEN_REFERENCE_DRAW_PROTOCOL.md
docs/theory/CERTGEN_MULTIPLICITY_FAMILY_PROTOCOL.md
```

Repair all defects discovered.

---

# 8. Phase C — Build the CVPR experiment ontology

CertGen needs a unified experiment model rather than scattered V6–V9 scripts.

Implement typed registries and schemas for:

## 8.1 Benchmarks

Create:

```text
registry/cvpr/benchmark_registry.yaml
```

Fields:

```text
benchmark_id
display_name
domain
resolution
conditioning
reference_source
reference_split
license
expected_reference_count
download_size_estimate
official_url_placeholder
local_source_formats
feature_spaces_supported
evaluation_protocols
known_preprocessing_risks
cvpr_relevance
execution_tier
status
blocker
```

Include planning entries for:

- CIFAR-10 as pilot/debug benchmark;
- FFHQ or CelebA-HQ as higher-resolution face benchmark;
- ImageNet class-conditional or another feasible recognized benchmark;
- one text-to-image prompt/image benchmark using public prompts and open/released samples;
- optional LSUN-style domain;
- optional video-generation extension.

Do not mark a benchmark ready unless inputs and licenses are verified.

## 8.2 Models and released sample sources

Create:

```text
registry/cvpr/model_registry.yaml
```

Fields:

```text
model_id
display_name
family
benchmark_id
task
architecture
checkpoint_or_sample_source
revision
license
authentication_required
released_samples_available
checkpoint_available
sample_count_available
resolution
conditioning
generation_cost_estimate
feature_only_possible
adapter
preflight_required
status
blocker
cvpr_recognition
```

Include candidate families where feasible:

- DDPM;
- ADM/guided diffusion;
- EDM;
- flow matching;
- latent diffusion;
- DiT;
- consistency/accelerated generation;
- StyleGAN variants;
- BigGAN where relevant;
- text-to-image open models;
- sampler/checkpoint variants.

No fabricated availability.

## 8.3 Feature spaces

Create:

```text
registry/cvpr/feature_space_registry.yaml
```

At minimum plan:

- Inception;
- CLIP;
- DINOv2 or an equivalent modern self-supervised visual encoder.

Fields:

```text
feature_space_id
model_identifier
revision
package
expected_dimension
input_resolution
resize
crop
interpolation
pixel_range
normalization
feature_normalization
precision
batch_size_default
device_support
license
cache_schema_version
status
```

## 8.4 Comparison taxonomy

Create:

```text
registry/cvpr/comparison_registry.csv
```

Fields:

```text
comparison_id
benchmark_id
model_a
model_b
comparison_type
source_of_pair
prospective_or_posthoc
primary_or_secondary
expected_gap_class
feature_spaces
metrics
sample_budgets
family_id
status
blocker
```

Comparison types must include:

- null reference split versus reference split;
- same model/checkpoint independent seeds;
- obvious-gap corruption control;
- controlled degradation ladder;
- same model different sampler;
- same family different checkpoint;
- cross-family contestable pair;
- real reported pair;
- preprocessing sensitivity pair;
- feature-space disagreement candidate.

No result-driven pair selection during this build pass.

## 8.5 Preregistration schema

Build a machine-readable preregistration object supporting:

```text
study_id
version
primary_question
primary_outcomes
secondary_outcomes
benchmarks
models
model_pairs
feature_spaces
metrics
kernel
bandwidth_protocol
alpha
multiplicity_families
sample_budgets
stopping_rule
reference_draw_protocol
exclusion_rules
failure_rules
resume_rules
missing-data rules
censoring rules
claim thresholds
scale-up rules
pivot rules
configuration_hash
frozen
```

Create:

```text
configs/cvpr/certgen_cvpr_preregistration_template.yaml
docs/experiments/CERTGEN_CVPR_PREREGISTRATION_GUIDE.md
```

---

# 9. Phase D — Build the canonical execution architecture

Create one canonical interface.

Preferred shape:

```bash
python3 -m certgen status
python3 -m certgen next-action
python3 -m certgen validate reference
python3 -m certgen materialize reference
python3 -m certgen preflight checkpoints
python3 -m certgen package generation
python3 -m certgen import generation
python3 -m certgen package features
python3 -m certgen import features
python3 -m certgen validate caches
python3 -m certgen sanity
python3 -m certgen certify
python3 -m certgen rank
python3 -m certgen analyze
python3 -m certgen figures
python3 -m certgen audit
```

Use existing commands where better. Do not duplicate business logic.

## 9.1 Stage state machine

Implement an explicit stage machine:

```text
REFERENCE_SOURCE_MISSING
REFERENCE_SOURCE_VALID
REFERENCE_MATERIALIZED
CHECKPOINT_PREFLIGHT_REQUIRED
CHECKPOINT_PREFLIGHT_IMPORTED
GENERATION_INPUT_READY
GENERATION_OUTPUT_REQUIRED
GENERATION_IMPORTED
FEATURE_INPUT_READY
FEATURE_OUTPUT_REQUIRED
FEATURES_IMPORTED
CACHE_VALIDATION_REQUIRED
METRIC_REPRODUCTION_REQUIRED
SANITY_GATES_REQUIRED
FIRST_PILOT_READY
FIRST_PILOT_COMPLETE_NONCLAIM
MULTIBENCHMARK_EXPANSION_READY
CVPR_EVIDENCE_GATES_PENDING
```

Each transition must define:

```text
required_inputs
validator
output_artifacts
failure_status
next_action
evidence_class
claim_permission
```

## 9.2 Exact next-action engine

Repair or extend the current engine so it produces exactly one next action:

```text
status
reason
exact command
execution location
required input
expected output
success validator
estimated runtime
CPU or GPU
Kaggle or local
network requirement
evidence class
claim permission
blocker
```

## 9.3 Run identity

All real and planned runs must use a stable `run_id`, for example:

```text
benchmark__stage__scale__feature-space__config-hash__timestamp
```

Implement:

```text
run_id
configuration_hash
parent_run_ids
artifact lineage
```

## 9.4 Artifact registry

Create or strengthen:

```text
data/artifact_registry.jsonl
```

Fields:

```text
artifact_id
run_id
path
artifact_type
stage
benchmark
model
feature_space
metric
source
source_hash
artifact_hash
schema_version
created_at
validation_status
evidence_class
claim_allowed
parent_artifacts
notes
```

## 9.5 Atomicity and resumability

All stage outputs must use:

- temporary files;
- atomic rename;
- explicit completion markers;
- per-shard status;
- idempotent reruns;
- no silent overwrite;
- `--force` only when explicit;
- raw input preservation;
- deterministic merges.

---

# 10. Phase E — Repair the CIFAR reference onramp

The current blocker is the missing CIFAR source.

Support these input forms:

- official `cifar-10-python.tar.gz`;
- extracted Python batch directory;
- torchvision cache;
- validated image-folder export;
- user-provided ZIP/TAR containing an official structure.

The onramp must support:

```bash
python3 -m certgen validate reference --source <path> --explain
python3 -m certgen materialize reference --source <path>
python3 -m certgen next-action
```

Validation must check:

- source structure;
- archive safety;
- official filenames;
- expected counts;
- labels;
- train/test splits;
- dimensions;
- channels;
- duplicate IDs;
- deterministic ordering;
- content hashes;
- source license metadata;
- archive hash;
- materialization manifest.

Create fixture tests for accepted and rejected forms.

The final next-action report must say:

```text
accepted input forms
searched locations
rejected candidates and reasons
exact user action
exact next command
expected output
success status
```

Do not download CIFAR.

---

# 11. Phase F — Build production-grade Kaggle T4×2 checkpoint preflight notebook

Create:

```text
notebooks/kaggle/certgen_cvpr_checkpoint_preflight_t4x2.ipynb
```

This notebook is non-evidence.

## 11.1 Requirements

The notebook must:

1. print Python, OS, CUDA, PyTorch, package, and GPU information;
2. verify exactly two visible GPUs when T4×2 is requested;
3. support graceful single-GPU fallback with an explicit warning;
4. check disk space;
5. configure model cache paths;
6. install or verify pinned dependencies;
7. validate package versions;
8. read the model registry/config from the uploaded input dataset;
9. isolate each model preflight;
10. pin each worker explicitly to a GPU;
11. load each checkpoint/revision;
12. generate only 1–4 images per model;
13. validate image shape, mode, range, and decode;
14. capture scheduler/pipeline class;
15. capture dependency and revision metadata;
16. write per-model status JSON;
17. preserve stack traces;
18. create an integrity manifest;
19. package outputs into a deterministic ZIP;
20. label everything:

```text
non_evidence_preflight
run_log_only
claim_allowed=false
```

## 11.2 Parallel model

Use two independent worker processes:

```text
GPU 0: assigned model subset
GPU 1: assigned model subset
```

Avoid unnecessary distributed/NCCL complexity.

## 11.3 Resume

The notebook must:

- skip completed successful model preflights only when configuration hashes match;
- rerun failed models explicitly;
- never treat a partial run as success;
- preserve earlier logs;
- produce exact rerun instructions.

## 11.4 Output

Produce:

```text
certgen_cvpr_checkpoint_preflight_<run_id>.zip
```

with:

```text
status.json
environment.json
gpu_info.json
per_model/
logs/
tiny_images/
integrity_manifest.json
copyback_instructions.md
```

---

# 12. Phase G — Build production-grade Kaggle T4×2 generation notebooks

Create a canonical notebook factory and at least these notebooks:

```text
notebooks/kaggle/certgen_cvpr_cifar10_generation_t4x2_1k.ipynb
notebooks/kaggle/certgen_cvpr_generation_t4x2_generic.ipynb
```

The generic notebook must be config-driven and suitable for future benchmark/model lanes.

## 12.1 Requirements

The notebooks must:

- validate input package and configuration hash;
- verify preflight success;
- print environment and GPU status;
- pin workers to GPU 0 and GPU 1;
- create deterministic non-overlapping seed ranges;
- support model-level and shard-level concurrency;
- use independent worker processes;
- record batch size;
- record scheduler;
- record inference steps;
- record guidance scale where applicable;
- record class/prompt conditioning;
- record precision;
- record model revision;
- record package versions;
- record per-image sample ID;
- record seed;
- record prompt/class ID;
- record image hash;
- record image path;
- record wall time;
- detect duplicates;
- decode every image before completion;
- validate dimensions and color mode;
- write atomic shards;
- support resume;
- support failed-shard rerun;
- block final success on partial failure;
- preserve logs;
- generate a deterministic merged manifest;
- create an integrity manifest;
- create a deterministic output ZIP.

## 12.2 Two-GPU allocation

For a simple 1k lane:

```text
GPU 0: seeds 0–499
GPU 1: seeds 500–999
```

For configurable lanes:

- partition by deterministic shard IDs;
- keep seed ranges disjoint;
- ensure reruns preserve assignments;
- verify no overlap after merge.

## 12.3 Scale lanes

Support:

```text
smoke: 4–16 images per model
pilot: 1k images per model
medium: 10k images per model
full: 50k images per model
```

Only `smoke` is allowed in checkpoint preflight. Do not run any lane now.

## 12.4 Output

Produce:

```text
certgen_cvpr_generation_<benchmark>_<scale>_<run_id>.zip
```

with:

```text
status.json
environment.json
configuration.yaml
per_model/
per_shard/
images/
manifests/
logs/
integrity_manifest.json
copyback_instructions.md
```

---

# 13. Phase H — Build production-grade Kaggle T4×2 feature-extraction notebooks

Create:

```text
notebooks/kaggle/certgen_cvpr_feature_extraction_t4x2_1k.ipynb
notebooks/kaggle/certgen_cvpr_feature_extraction_t4x2_generic.ipynb
```

Support the feature spaces defined in the registry, at minimum:

- Inception;
- CLIP;
- DINOv2 or another explicitly selected modern self-supervised encoder.

## 13.1 Input validation

Before GPU work, validate:

- package hash;
- configuration hash;
- image count by role;
- model count;
- benchmark;
- sample IDs;
- image hashes;
- duplicate IDs;
- missing files;
- dimensions;
- color mode;
- role labels;
- reference draw-plan compatibility;
- preprocessing-lock configuration.

## 13.2 GPU parallelism

Use T4×2 through independent workers.

Allowed strategies:

```text
GPU 0: shard 0 of image roles
GPU 1: shard 1 of image roles
```

or:

```text
GPU 0: one extractor lane
GPU 1: another extractor lane
```

Choose the strategy based on memory and runtime planning. Make it config-driven.

## 13.3 Extractor contract

For every extractor record:

```text
extractor_id
model_identifier
revision
package versions
input preprocessing
resize
crop
interpolation
pixel range
normalization
feature normalization
precision
batch size
output dimension
device
```

## 13.4 Output contract

For every role and shard produce:

```text
features.npy or features.npz
sample_ids
role
model_id
benchmark
feature_space
preprocessing_hash
extractor_hash
source_manifest_hash
configuration_hash
finite_check
dimension_check
status
```

## 13.5 Merge

The merge must be:

- deterministic;
- sample-ID based;
- order-independent;
- duplicate rejecting;
- schema validating;
- atomic;
- resumable.

## 13.6 Cache schema

Strengthen `cache_v2` or introduce a backward-compatible next schema only if necessary.

It must detect:

- incompatible feature dimensions;
- mixed extractor revisions;
- mixed preprocessing;
- missing IDs;
- duplicate IDs;
- role mismatch;
- partial shards;
- wrong source manifest;
- stale configuration;
- NaNs/infinities;
- non-deterministic ordering;
- mixed precision when prohibited.

## 13.7 Output ZIP

Produce:

```text
certgen_cvpr_features_<benchmark>_<scale>_<run_id>.zip
```

with:

```text
status.json
environment.json
extractor_registry_snapshot.yaml
preprocessing_locks/
shards/
merged/
sidecars/
logs/
integrity_manifest.json
copyback_instructions.md
```

---

# 14. Phase I — Build secure import and repair workflows

Create or consolidate canonical importers:

```bash
python3 -m certgen import preflight <zip>
python3 -m certgen import generation <zip>
python3 -m certgen import features <zip>
```

## 14.1 ZIP safety

Protect against:

- path traversal;
- absolute paths;
- symlinks;
- executables;
- nested archives;
- duplicate paths;
- unsupported files;
- zip bombs or excessive expansion;
- overwriting existing runs;
- invalid schema;
- missing status;
- partial success;
- hash mismatch;
- configuration mismatch;
- evidence-language injection.

## 14.2 Raw preservation

Preserve each original ZIP under an immutable or hash-addressed path.

Record:

```text
source ZIP hash
import time
run ID
schema
validation result
repair actions
```

## 14.3 Repair reports

When import fails, produce:

```text
missing files
failed shards
hash mismatches
schema mismatches
exact repair command
whether Kaggle rerun is needed
whether local repair is safe
```

## 14.4 Tests

Add fixture archives for:

- valid preflight;
- valid generation;
- valid features;
- partial output;
- corrupt archive;
- malicious traversal;
- duplicate files;
- wrong configuration;
- missing manifest;
- incompatible schema;
- incorrect hashes.

---

# 15. Phase J — Build metric reproduction and sanity gates

Do not run real metrics now. Build the complete gates.

## 15.1 Metric reproduction gate

A metric reproduction must verify:

- exact reference set;
- exact generated set;
- exact sample count;
- exact feature extractor;
- exact preprocessing;
- exact metric convention;
- exact kernel and bandwidth;
- expected numerical tolerance;
- provenance of the target value.

For metrics without a trusted published target, label:

```text
cross_implementation_consistency
not_external_reproduction
```

## 15.2 Sanity families

Build config-driven sanity analyses for:

### Null controls

- reference split versus reference split;
- same model/checkpoint independent samples;
- repeated batching;
- repeated shard merge.

### Obvious-gap controls

- reference versus severe corruption;
- high-quality versus intentionally degraded samples;
- controlled noise/blur/compression ladder.

### Direction controls

- corruption severity should move in the expected direction in aggregate;
- certificate direction should match known controlled ordering when decided.

### Protocol controls

- preprocessing lock mismatch must fail;
- feature-space mismatch must fail;
- bandwidth mismatch must fail;
- reference population mismatch must fail.

## 15.3 Gate result schema

Every gate result must contain:

```text
gate_id
run_id
inputs
configuration_hash
status
measured_values
tolerances
failure_reason
evidence_class
claim_allowed
```

Synthetic sanity results remain non-paper evidence.

---

# 16. Phase K — Build certificate and samples-to-decision pipeline

Create a canonical certificate engine.

Preferred interface:

```bash
python3 -m certgen certify \
  --study <preregistration.yaml> \
  --family <family_id> \
  --features <artifact_id>
```

## 16.1 Certificate output

For each pair:

```text
comparison_id
benchmark
model_a
model_b
feature_space
metric
kernel
bandwidth
alpha_total
alpha_pair
sample_budget
decision
direction
first_decision_n
censored
lower_bound
upper_bound
configuration_hash
reference_draw_hash
feature_cache_hashes
family_id
evidence_class
claim_allowed
limitations
```

Possible decisions:

```text
A_BETTER
B_BETTER
UNDECIDED_AT_BUDGET
INVALID_INPUT
BLOCKED_ASSUMPTION
```

## 16.2 Resume safety

The certificate engine must:

- preserve alpha accounting;
- preserve stream order;
- preserve reference draw;
- prevent reusing a stopped stream as a new independent test;
- detect changed configurations;
- reproduce identical results from identical inputs.

## 16.3 Samples-to-decision

Implement correct handling of:

- decided comparisons;
- censored undecided comparisons;
- maximum budget;
- first crossing;
- comparison across budgets;
- summary statistics that do not condition only on decided pairs without disclosure.

Build Kaplan–Meier-style or another appropriate censored summary only if statistically justified. Otherwise produce explicit censored distributions.

## 16.4 Output classes

Separate:

```text
raw certificate artifacts
pilot summary
multi-benchmark analysis
paper-approved evidence
```

No paper approval in this build pass.

---

# 17. Phase L — Build certified partial ranking system

CVPR readiness requires a visual ranking object.

Create:

```bash
python3 -m certgen rank \
  --family <family_id> \
  --certificate-dir <path>
```

## 17.1 Ranking graph

Represent:

- nodes = visual generative models;
- directed certified edges;
- unresolved edges;
- contradictory feature-space edges;
- missing comparisons;
- excluded comparisons;
- family-wise alpha;
- configuration hash.

## 17.2 Do not force total order

Outputs must support:

- partial order;
- incomparable groups;
- unresolved pairs;
- strongly connected contradictions across feature spaces;
- feature-space-specific graphs;
- consensus graph only under a predefined rule.

## 17.3 Ranking outputs

Create machine-readable and plotting-ready outputs:

```text
ranking_graph.json
ranking_edges.csv
unresolved_pairs.csv
feature_disagreements.csv
ranking_summary.json
```

## 17.4 Ranking validity gate

The system must reject:

- mixed multiplicity families;
- mixed alpha;
- mixed preprocessing;
- mixed reference populations;
- mixed benchmarks;
- incompatible feature spaces in a single graph without an explicit aggregation protocol.

---

# 18. Phase M — Build CVPR visual-analysis and figure factory

Do not create empirical figures now. Build scripts and schemas that refuse to run without approved real artifacts.

Create a figure factory capable of producing:

## 18.1 Headline partial-ranking figure

- point-estimate leaderboard;
- certified partial graph;
- resolved edges;
- unresolved edges;
- feature-space contradictions.

## 18.2 Anytime trajectory figure

- running estimate;
- lower/upper confidence sequence;
- decision boundary;
- first crossing;
- maximum budget.

## 18.3 Decidedness-versus-budget figure

- percentage decided;
- percentage undecided;
- benchmark;
- feature space;
- budget.

## 18.4 Samples-to-decision figure

- decided comparisons;
- censored comparisons;
- budget distribution;
- feature-space stratification.

## 18.5 Protocol-sensitivity figure

- preprocessing;
- bandwidth;
- reference split;
- block/sample construction;
- feature extractor.

## 18.6 Compute-savings figure

- fixed budget;
- actual first-decision budget;
- saved images;
- saved feature-extraction work;
- saved generation work where applicable.

## 18.7 Visual model-pair gallery

Support curated panels showing:

- reported scores;
- point-estimate direction;
- certificate decision;
- first-decision sample count;
- feature-space disagreement;
- representative images.

The gallery must not imply that selected images prove distribution-level claims.

## 18.8 Controlled failure gallery

Show:

- point estimate suggests winner but certificate is undecided;
- feature spaces disagree;
- preprocessing changes direction;
- null pair falsely appears separated under naive peeking;
- fixed budget wastes compute for obvious gaps.

## 18.9 Figure contracts

Each figure script must require:

```text
approved input artifacts
schema
configuration hash
claim gate status
output path
caption metadata
limitations
```

Create:

```text
certgen/visualization/
paper/figures/README.md
paper/CERTGEN_CVPR_FIGURE_CONTRACTS.md
```

---

# 19. Phase N — Build literature and leaderboard audit infrastructure

Do not perform the real literature audit now unless a fully cited dataset already exists.

Build a schema and ingestion pipeline.

## 19.1 Claim registry

Create:

```text
registry/cvpr/published_claim_registry.csv
```

Fields:

```text
claim_id
paper_title
paper_year
venue
paper_url_or_identifier
benchmark
model_a
model_b
reported_metric
reported_a
reported_b
sample_count
feature_extractor
preprocessing
released_samples_available
checkpoint_available
license
reproduction_possible
selection_reason
prospective_status
notes
```

## 19.2 Inclusion protocol

Create a protocol defining:

- date window;
- venue set;
- benchmark set;
- metric set;
- comparison eligibility;
- sample/checkpoint availability;
- exclusion rules;
- duplicate claims;
- same-paper multiple comparisons;
- missing preprocessing;
- irreproducible claims;
- prospective freeze.

## 19.3 Audit outputs

Plan for:

- fraction reproduced;
- fraction eligible;
- fraction decided;
- fraction undecided;
- direction reversals;
- feature-space disagreements;
- ranking changes;
- compute budget implications.

Do not call an original paper wrong merely because CertGen is undecided.

---

# 20. Phase O — Build multi-benchmark and multi-scale execution matrix

Create:

```text
configs/cvpr/execution_matrix.yaml
```

The matrix must classify all planned runs by:

- benchmark;
- model;
- feature space;
- scale;
- stage;
- CPU/GPU;
- Kaggle/local;
- expected input;
- expected output;
- prerequisite;
- priority;
- runtime estimate;
- memory estimate;
- resumability;
- evidence class.

## 20.1 Required scales

```text
preflight
1k pilot
10k medium
50k full
```

## 20.2 Required run classes

### Class A — local validation

- compilation;
- tests;
- schemas;
- synthetic statistical checks;
- notebook static analysis;
- paper firewall.

### Class B — reference materialization

- local CPU;
- dataset dependent;
- no GPU.

### Class C — checkpoint preflight

- Kaggle T4×2;
- non-evidence;
- tiny generation only.

### Class D — generation

- Kaggle T4×2;
- per benchmark/model/scale.

### Class E — feature extraction

- Kaggle T4×2;
- per feature space.

### Class F — import and validation

- local CPU.

### Class G — metric reproduction and sanity

- local CPU, possibly high RAM.

### Class H — certificate pilot

- local CPU.

### Class I — ranking and sensitivity analyses

- local CPU.

### Class J — figure generation

- local CPU.

### Class K — literature audit

- local CPU/manual curation.

### Class L — paper-evidence promotion

- local CPU;
- claim gate;
- only after all evidence requirements pass.

---

# 21. Phase P — Runtime and resource planner

Create a planner that reports estimates for each run.

Every estimate must be labeled:

```text
planning estimate
hardware-dependent
not an empirical project result
```

## 21.1 Runtime model

Include:

- fixed setup time;
- model download/cache time;
- generation throughput;
- feature throughput;
- image count;
- batch size;
- number of GPUs;
- shard count;
- merge time;
- local validation time;
- expected disk use;
- expected RAM/VRAM.

## 21.2 Initial planning ranges

Use conservative ranges and allow configuration overrides.

Examples, subject to model and benchmark:

```text
checkpoint preflight:
5–30 minutes total

CIFAR 1k generation, 3 small models, T4×2:
30 minutes–3 hours

1k Inception extraction:
5–30 minutes

1k CLIP extraction:
10–45 minutes

1k DINO extraction:
10–60 minutes

CPU import and validation:
seconds–tens of minutes

CPU certificate pilot:
seconds–minutes

10k generation:
model-dependent, possibly 1–8 hours per model

50k generation:
model-dependent, possibly multiple Kaggle sessions
```

Do not present these as measured.

## 21.3 Session planner

For Kaggle limits, generate:

- estimated number of sessions;
- per-session shard plan;
- expected ZIP sizes;
- copy-back checkpoints;
- resume points;
- failure recovery commands.

---

# 22. Phase Q — Notebook static analyzer v3

Build or strengthen a notebook analyzer that checks:

- valid JSON notebook;
- no embedded secrets;
- no absolute private paths;
- expected title and non-evidence labels;
- GPU visibility checks;
- explicit T4×2 handling;
- GPU pinning;
- deterministic seed partitioning;
- resume logic;
- atomic writes;
- per-shard status;
- blocked status on partial failure;
- integrity manifest;
- output ZIP;
- copy-back instructions;
- runtime logging;
- package-version capture;
- configuration-hash validation;
- no certificate generation inside GPU notebooks;
- no paper evidence generation;
- no `claim_allowed=true`;
- no unexplained hidden outputs;
- no stale hardcoded model IDs outside config;
- idempotence safeguards.

Add fixture tests that inspect notebook structure.

---

# 23. Phase R — Codebase repair and architecture consolidation

Inspect for:

- duplicated V6–V9 logic;
- inconsistent status names;
- hardcoded paths;
- shell/Python duplication;
- stale wrappers;
- dead code;
- broad exception handling;
- silent fallbacks;
- non-atomic writes;
- weak typing in the statistical and artifact core;
- schema drift;
- nondeterministic iteration;
- inconsistent hash calculation;
- mixed time formats;
- recursive tests;
- artifact overwrite risk;
- weak CLI errors.

## 23.1 Canonicalize, do not delete

Where possible:

- route legacy wrappers through canonical implementations;
- mark legacy scripts deprecated;
- preserve backward compatibility;
- document archive candidates;
- do not destructively delete historical prompt packs or reports.

## 23.2 Typing priority

The repository currently has reported mypy debt.

Do not spend the entire pass eliminating every historical typing error.

Prioritize typing and schema correctness in:

- statistical core;
- reference draw plan;
- feature cache;
- artifact registry;
- importers;
- stage state machine;
- exact next-action engine;
- certificate objects;
- ranking graph;
- CVPR registries;
- canonical CLI.

Document remaining debt.

## 23.3 Public release surface

Create or update:

```text
release/CERTGEN_CVPR_PUBLIC_RELEASE_MANIFEST.txt
release/CERTGEN_CVPR_INTERNAL_ARCHIVE_CANDIDATES.txt
docs/engineering/CERTGEN_CVPR_CANONICAL_ARCHITECTURE.md
docs/engineering/CERTGEN_CVPR_CLI_REFERENCE.md
```

The public release should eventually expose:

- core package;
- typed configs;
- canonical CLI;
- essential notebooks;
- tests;
- paper sources;
- reproducibility scripts;
- validated docs;
- license;
- citation file.

Do not expose prompt-pack clutter in the default public package.

---

# 24. Phase S — CVPR paper scaffold and evidence firewall

Do not write fake results.

Repair the paper so it is structurally ready for real evidence.

## 24.1 Paper identity

The paper should be written as a vision evaluation paper.

Suggested working title family:

```text
CertGen: Certified Partial Rankings for Visual Generative Models
```

Alternative:

```text
Are Visual Generative-Model Improvements Statistically Decided?
```

Alternative:

```text
How Many Images Until We Know? Anytime-Valid Comparison of Visual Generators
```

Choose a working title but mark it provisional.

## 24.2 Recommended structure

1. Introduction
2. Why point-estimate visual-generation leaderboards overstate certainty
3. Problem formulation and certificate contract
4. Valid bounded-kernel comparison method
5. Certified partial rankings
6. CVPR-oriented experimental protocol
7. Controlled validity experiments
8. Real visual generative-model audit
9. Cross-representation and protocol sensitivity
10. Compute and samples-to-decision analysis
11. Limitations
12. Related work
13. Conclusion

## 24.3 Main claim hierarchy

Create explicit placeholders for:

- methodological validity;
- optional-stopping robustness;
- family-wise control;
- certified partial rankings;
- empirical decidedness;
- cross-feature consistency;
- compute savings;
- literature audit;
- limitations.

No empirical statement may appear without an approved artifact ID.

## 24.4 Paper firewall

Strengthen the firewall so it blocks:

- placeholder numbers;
- synthetic results;
- run-log values;
- planning runtimes;
- notebook preflight images;
- fixture results;
- unapproved certificates;
- single-benchmark pilot overgeneralization;
- unsupported FID claims;
- unsupported metric-agnostic language;
- unsupported total-ranking claims;
- `claim_allowed=true` without complete lineage.

## 24.5 Paper contracts

Create:

```text
paper/CERTGEN_CVPR_CLAIM_CONTRACT.md
paper/CERTGEN_CVPR_RESULT_TABLE_CONTRACTS.md
paper/CERTGEN_CVPR_FIGURE_CONTRACTS.md
paper/CERTGEN_CVPR_PAPER_BUILD_REPORT.md
```

Compile the paper after structural repairs.

---

# 25. Phase T — CVPR reviewer simulation and gap closure

Write adversarial reviews from:

1. a generative-model evaluation reviewer;
2. a statistics/sequential-inference reviewer;
3. a CVPR vision reviewer;
4. a reproducibility reviewer;
5. a skeptical “incremental/KID with stopping rule” reviewer.

For each include:

```text
summary
strengths
major concerns
fatal concern
required theory
required experiments
required visual evidence
required baselines
required ablations
current score
score after successful execution
```

Build a repair matrix.

The build pass should close all pre-execution reviewer concerns that can be closed without real runs.

Examples:

- missing experimental schemas;
- unclear model-pair selection;
- weak feature-space plan;
- missing baseline contracts;
- missing visual figure plan;
- missing failure protocol;
- unsupported paper wording;
- missing multiplicity family definition;
- unclear FID boundary;
- unclear partial-ranking logic.

Create:

```text
paper/CERTGEN_CVPR_REVIEWER_SIMULATION.md
paper/CERTGEN_CVPR_REVIEWER_REPAIR_MATRIX.csv
```

---

# 26. Phase U — Baseline and ablation contracts

Prepare executable configs for eventual comparisons.

## 26.1 Statistical baselines

Plan:

- fixed-n point estimate only;
- fixed-n bootstrap interval;
- naive repeated peeking;
- Bonferroni fixed-n testing;
- CertGen time-uniform route;
- closest applicable sequential-kernel baseline where implementable.

Do not claim superiority until run.

## 26.2 Evaluation baselines

Plan descriptive comparison across:

- FID/FD;
- polynomial KID;
- bounded RBF-MMD;
- CLIP-feature RBF-MMD;
- DINO-feature RBF-MMD;
- precision/recall or density/coverage where feasible.

Only bounded routes receive certificates unless separately justified.

## 26.3 Ablations

Prepare configs for:

- alpha;
- maximum budget;
- bandwidth;
- bandwidth-selection protocol;
- reference split;
- reference sampling;
- feature space;
- normalization;
- preprocessing;
- Bonferroni family size;
- model-pair family;
- seed partition;
- 1k/10k/50k scale;
- resume boundary;
- controlled dependence violation as a synthetic diagnostic.

## 26.4 Negative controls

Prepare:

- null pairs;
- duplicate model independent seeds;
- identical cached features;
- shuffled labels where relevant;
- corrupted manifests;
- mixed preprocessing rejection;
- mixed reference rejection.

---

# 27. Phase V — Final local-safe verification

After implementation, run the broadest safe local verification.

At minimum:

```text
Python compile
package imports
full non-recursive default tests
fast CI lane
statistical fast lane
artifact-contract lane
extended synthetic lane where reasonable
notebook static analyzer
notebook syntax/structure checks
registry/schema validation
CLI smoke tests
paper firewall
secrets scan
release scan
ruff
mypy on critical modules
paper compilation
git diff --check
final CVPR pre-execution audit
```

Record exact commands and exit codes.

No test may download data or models unexpectedly.

No test may create claim-bearing evidence.

No test may modify raw artifacts.

---

# 28. Final top-level status taxonomy

Choose exactly one:

```text
CVPR_BUILD_FAILED_REPOSITORY_INCONSISTENT
CVPR_BUILD_REPAIRS_COMPLETE_TESTS_FAILING
CVPR_LOCAL_CORE_BLOCKED_BY_THEORY
CVPR_PREEXECUTION_READY_BLOCKED_BY_REFERENCE_INPUT
CVPR_REFERENCE_READY_CHECKPOINT_PREFLIGHT_REQUIRED
CVPR_CHECKPOINT_PREFLIGHT_READY
CVPR_1K_GENERATION_READY
CVPR_FEATURE_EXTRACTION_READY
CVPR_METRIC_SANITY_READY
CVPR_FIRST_PILOT_READY
CVPR_FIRST_PILOT_COMPLETE_NONCLAIM
CVPR_EVIDENCE_EXPANSION_READY
CVPR_PAPER_EVIDENCE_READY
```

Expected likely status without real input:

```text
CVPR_PREEXECUTION_READY_BLOCKED_BY_REFERENCE_INPUT
```

Do not force this status if the repository does not deserve it.

Also report sub-statuses:

```text
theory
software
registries
reference
checkpoint preflight
generation
feature extraction
cache contracts
metric reproduction
sanity
certificate
partial ranking
visualization
literature audit
paper
release
```

---

# 29. Mandatory final single-file run handbook

Create:

```text
CERTGEN_CVPR_COMPLETE_EXECUTION_AND_RUN_HANDBOOK.md
```

This is one of the most important deliverables.

It must be self-contained and detailed enough that another agent or researcher can execute all remaining runs in the correct order.

## 29.1 Required handbook sections

### A. Current exact status

- verified tests;
- verified audits;
- current blocker;
- exact next command;
- current evidence state.

### B. Hardware and environment assumptions

- local Mac/CPU;
- Kaggle T4×2;
- optional single T4 fallback;
- disk;
- RAM;
- VRAM;
- network;
- package versions.

### C. Complete run classification

Use a table with:

```text
run_id_template
run_name
class
priority
purpose
location
CPU_or_GPU
GPU_count
input
output
prerequisites
command_or_notebook
planning_runtime
planning_disk
planning_RAM
planning_VRAM
resumable
failure_recovery
evidence_class
claim_permission
completion_test
```

### D. Classify every run into

```text
LOCAL_CPU_VALIDATION
LOCAL_CPU_REFERENCE
KAGGLE_T4X2_PREFLIGHT
KAGGLE_T4X2_GENERATION
KAGGLE_T4X2_FEATURE_EXTRACTION
LOCAL_CPU_IMPORT
LOCAL_CPU_METRIC_REPRODUCTION
LOCAL_CPU_SANITY
LOCAL_CPU_CERTIFICATE
LOCAL_CPU_PARTIAL_RANKING
LOCAL_CPU_SENSITIVITY
LOCAL_CPU_FIGURES
LOCAL_CPU_LITERATURE_AUDIT
LOCAL_CPU_PAPER_GATE
```

### E. Exact critical path to first pilot

Include every command in order:

1. provide reference;
2. validate reference;
3. materialize reference;
4. build checkpoint-preflight input;
5. run Kaggle preflight;
6. copy back ZIP;
7. import preflight;
8. build generation input;
9. run Kaggle 1k generation;
10. copy back ZIP;
11. import generation;
12. build feature input;
13. run Kaggle feature extraction;
14. copy back ZIP;
15. import features;
16. validate cache-v2;
17. reproduce metrics;
18. run null controls;
19. run obvious-gap controls;
20. freeze Bonferroni family;
21. run first certificate pilot;
22. build pilot partial ranking;
23. render pilot figures;
24. run evidence gate;
25. stop and interpret before scaling.

### F. Scale-up path

Define gates for:

```text
1k → 10k
10k → 50k
CIFAR → second benchmark
second benchmark → third benchmark
image core → optional video
pilot artifact → paper evidence
```

### G. Expected runtime tables

Separate:

- local CPU;
- Kaggle T4×2;
- data transfer;
- feature extraction;
- metric/certificate analysis;
- figure generation.

All runtimes labeled as planning estimates.

### H. Kaggle notebook instructions

For each notebook include:

- required input dataset ZIP;
- exact Kaggle accelerator;
- internet setting;
- expected GPU visibility;
- cells to run;
- resume behavior;
- output ZIP;
- how to download;
- how to import locally;
- exact validation command;
- failure recovery.

### I. Run dependency graph

Include a textual DAG.

### J. Evidence promotion rules

For every stage say whether it produces:

```text
planning artifact
run log
cache artifact
sanity artifact
pilot artifact
paper evidence
```

### K. Stop conditions

Stop immediately when:

- provenance fails;
- cache schema fails;
- feature dimensions mismatch;
- preprocessing lock mismatches;
- checkpoint preflight fails;
- metric reproduction fails;
- null calibration fails;
- obvious-gap direction fails;
- reference-draw assumptions fail;
- family registry is not frozen;
- paper firewall fails.

### L. Exact next action

One singular command with expected output and success status.

---

# 30. Mandatory final master report

Create:

```text
CERTGEN_CVPR_MAX_PREEXECUTION_BUILD_REPORT.md
```

Required structure:

## 1. Executive verdict

State:

- what CertGen is now;
- whether the build succeeded;
- whether the statistical core remains valid;
- whether the repository is ready for execution;
- exact blocker;
- exact next command;
- how much closer it is to CVPR readiness;
- what still depends entirely on real evidence.

## 2. Reproduced baseline

- tests;
- audits;
- warnings;
- mypy;
- paper build;
- git state.

## 3. CVPR-readiness gaps found

- theory;
- code;
- architecture;
- notebooks;
- benchmark breadth;
- model breadth;
- feature spaces;
- ranking;
- visualizations;
- paper;
- release.

## 4. Repairs implemented

For each:

```text
finding
severity
root cause
change
files
tests
verification
remaining limitation
```

## 5. New systems built

Summarize:

- registries;
- canonical CLI;
- stage machine;
- artifact registry;
- reference onramp;
- preflight notebook;
- generation notebooks;
- feature notebooks;
- importers;
- cache validation;
- certificate engine;
- partial ranking;
- figure factory;
- literature audit;
- runtime planner;
- final run handbook.

## 6. Statistical validity

- exact route;
- assumptions;
- bounds;
- multiplicity;
- unsupported routes;
- remaining proof obligations.

## 7. CVPR experiment program

- pilot;
- minimum credible study;
- strong study;
- maximum-ceiling study.

## 8. Notebook readiness

For every notebook:

```text
static validation
fixture validation
T4×2 support
resume support
known unverified risks
real-run required
```

Never call notebooks errorless.

## 9. Verification

Exact commands and exit codes.

## 10. Remaining blockers

Separate:

```text
USER_INPUT_REQUIRED
KAGGLE_EXECUTION_REQUIRED
REAL_DATA_REQUIRED
REAL_MODEL_REQUIRED
REAL_FEATURES_REQUIRED
EMPIRICAL_RESULT_REQUIRED
PAPER_EVIDENCE_REQUIRED
OPTIONAL_POST_PILOT_UPGRADE
```

## 11. CVPR ceiling assessment

State what successful real results would be needed for:

```text
pilot only
workshop-level study
minimum credible CVPR submission
competitive CVPR submission
strong CVPR submission
maximum realistic ceiling
```

## 12. Stop-building verdict

Explicitly list:

```text
DO NOT BUILD BEFORE FIRST PILOT
ONLY BUILD IF EXECUTION FAILS
ONLY BUILD IF RESULTS JUSTIFY IT
POST_PILOT THEORY OPTIONS
POST_PILOT EMPIRICAL EXPANSIONS
```

## 13. Exact next action

One command.

---

# 31. Additional required deliverables

Create or update:

```text
docs/CERTGEN_CVPR_EXACT_NEXT_ACTION.md
docs/CERTGEN_CVPR_EXECUTION_CRITICAL_PATH.md
docs/CERTGEN_CVPR_MAXIMUM_RESEARCH_CEILING.md
docs/CERTGEN_CVPR_SINGLE_FILE_HANDOFF.md
docs/CERTGEN_CVPR_STAGE_STATE_MACHINE.md
docs/CERTGEN_CVPR_EVIDENCE_PROMOTION_POLICY.md

reports/CERTGEN_CVPR_REPAIR_CHANGELOG.md
reports/CERTGEN_CVPR_REMAINING_WORK_PRIORITIZED.md
reports/CERTGEN_CVPR_NOTEBOOK_READINESS.md
reports/CERTGEN_CVPR_RUN_CLASSIFICATION.csv

docs/experiments/CERTGEN_CVPR_MINIMUM_CREDIBLE_STUDY.md
docs/experiments/CERTGEN_CVPR_STRONG_STUDY.md
docs/experiments/CERTGEN_CVPR_MAXIMUM_CEILING_STUDY.md
docs/experiments/CERTGEN_CVPR_SCALE_UP_RULES.md
docs/experiments/CERTGEN_CVPR_LITERATURE_AUDIT_PROTOCOL.md

docs/execution/CERTGEN_CVPR_KAGGLE_T4X2_GUIDE.md
docs/execution/CERTGEN_CVPR_FAILURE_RECOVERY_PLAYBOOK.md
docs/execution/CERTGEN_CVPR_RUNTIME_AND_RESOURCE_PLAN.md
docs/execution/CERTGEN_CVPR_COPYBACK_AND_IMPORT_GUIDE.md

paper/CERTGEN_CVPR_CLAIM_CONTRACT.md
paper/CERTGEN_CVPR_FIGURE_CONTRACTS.md
paper/CERTGEN_CVPR_RESULT_TABLE_CONTRACTS.md
paper/CERTGEN_CVPR_REVIEWER_SIMULATION.md
paper/CERTGEN_CVPR_REVIEWER_REPAIR_MATRIX.csv
```

Avoid redundant prose. Link detailed reports from the master report.

---

# 32. Prioritization

Classify remaining work as:

```text
P0 — correctness, validity, or evidence-integrity blocker
P1 — required before first real pilot
P2 — required immediately after first pilot
P3 — required for a credible CVPR submission
P4 — required for a strong CVPR submission
P5 — optional maximum-ceiling extension
REJECTED — unnecessary pre-execution overengineering
```

Every task must contain:

```text
task_id
priority
owner
location
input
output
dependency
CPU_or_GPU
planning_runtime
evidence_produced
claim_permission
completion_test
```

---

# 33. Stop-building rule

This is critical.

After this pass, generic infrastructure development must stop.

Only build more before the first pilot if:

- a correctness defect is discovered;
- a statistical assumption cannot be enforced;
- an artifact contract is unsafe;
- a Kaggle notebook cannot be packaged or validated;
- a required execution stage is missing;
- the paper firewall fails;
- a real run reveals a concrete failure.

Do not create another broad upgrade version merely because more improvements are imaginable.

Do not build before the first pilot:

- complex e-BH theory;
- rigorous FID certification;
- video pipelines beyond a minimal config skeleton;
- large dashboards;
- web applications;
- distributed clusters;
- paid-cloud support;
- arbitrary metric plugins;
- automatic paper writing;
- excessive historical migration tooling;
- decorative reports;
- additional prompt packs.

---

# 34. Quality standard

Be skeptical and precise.

Do not confuse:

- tests with evidence;
- static notebook checks with successful GPU execution;
- model metadata with checkpoint load success;
- synthetic coverage with real-model conclusions;
- cache existence with metric validity;
- pilot results with general findings;
- partial ranking with total ranking;
- point estimates with certified decisions;
- repository maturity with paper maturity.

The final repository should be:

- statistically defensible;
- vision-native;
- config-driven;
- reproducible;
- resumable;
- claim-safe;
- Kaggle T4×2-ready;
- multi-benchmark-ready;
- multi-feature-space-ready;
- partial-ranking-ready;
- figure-ready;
- paper-structure-ready;
- execution-ready.

But it must remain honest:

```text
no real runs
no empirical claims
no claim_allowed=true
```

---

# 35. Completion condition

This task is complete only when:

1. the live repository has been inspected;
2. the baseline has been reproduced;
3. all locally repairable correctness defects have been repaired;
4. the canonical CVPR experiment architecture exists;
5. the benchmark/model/feature/comparison registries exist;
6. the stage state machine exists;
7. the exact next-action engine is correct;
8. the CIFAR reference onramp is robust;
9. checkpoint-preflight notebook exists and passes static/fixture checks;
10. generation notebooks exist and pass static/fixture checks;
11. feature notebooks exist and pass static/fixture checks;
12. import and repair workflows are secure;
13. cache contracts are enforced;
14. certificate and partial-ranking pipelines exist;
15. figure factory contracts exist;
16. paper claim/result/figure firewalls exist;
17. all local-safe tests and audits pass or failures are honestly reported;
18. `CERTGEN_CVPR_COMPLETE_EXECUTION_AND_RUN_HANDBOOK.md` exists;
19. `CERTGEN_CVPR_MAX_PREEXECUTION_BUILD_REPORT.md` exists;
20. the final status and one exact next command are reported.

Begin now by inspecting the live repository and reproducing the current baseline. Do not draft the final verdict before examining the implementation and running the available local-safe checks.
