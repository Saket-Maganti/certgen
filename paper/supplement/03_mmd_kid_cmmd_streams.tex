\section{MMD, KID, and CMMD Streams}

The clean-core certificate targets discrepancy estimators that can be represented as bounded or clipped stream terms. KID/MMD/CMMD-style streams are monitored through the declared confidence sequence. Feature extraction and preprocessing remain locked before real runs.
