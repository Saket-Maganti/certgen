\section{Reproducibility Details}

The artifact uses CPU-only tests by default. Real execution requires a provenance ledger, preprocessing lock, feature-cache validation, metric reproduction audit, first clean-core pilot, report rendering, and final audit. No paid APIs, paid GPUs, paid datasets, paid annotation, or hosted inference are required by the dry-run path.
