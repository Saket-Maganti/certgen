# Multiplicity validation namespace

Canonical hand-computable Bonferroni, fixed-split, Holm, and exploratory BH
checks live in `tests/icml2027/test_multiplicity_end_to_end.py`. BH is
exploratory and no synthetic result is model evidence. `claim_allowed=false`.
