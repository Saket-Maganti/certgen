# Bounded-stream validation namespace

The original scalar synthetic suite is reclassified as
`bounded_stream_validation`. Historical outputs at the ICML report root are
retained without relabeling. They validate bounded scalar confidence-sequence
behavior, not the high-dimensional production MMD path. `claim_allowed=false`.
