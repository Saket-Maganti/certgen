# Representation validation namespace

Representation-specific results remain separate from contract regression and
production MMD validation. Direction conflicts block consensus; they are never
silently averaged away. `claim_allowed=false`.
