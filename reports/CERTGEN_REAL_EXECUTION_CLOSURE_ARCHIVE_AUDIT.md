# CertGen Real-Execution Closure Archive Audit

The clean reproducibility archive includes README, LICENSE, CITATION metadata, package metadata, five canonical notebooks, core code, portable tests, CVPR registries, closure reports, protocols, and the run-ready handbook. Raw data, the root CIFAR candidate, model caches, generated images, quarantine, Git metadata, and local/private empirical outputs are excluded.

Final clean-extraction verification: `ARCHIVE_VERIFIED`; 834 members; package import exit 0; 12 portable tests passed; 5/5 notebooks passed static validation; the builder-faithful synthetic rehearsal passed. These results are release validation only, not empirical model evidence.

The non-circular adjacent archive manifest records the final archive SHA-256, exact member manifest, timestamps, interpreter, and verification outputs. `claim_allowed=false`.
